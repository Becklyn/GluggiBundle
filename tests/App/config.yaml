framework:
    secret: secret
    test: ~
    router: ~
    profiler:
        collect: false

twig:
    strict_variables: true
    default_path: '%kernel.project_dir%/tests/templates'

gluggi:
    layout_dir: 'layout'
